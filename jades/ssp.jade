//- UNCLASSIFIED

extends base
append base_body

	- proj = get(projs,{Task:"wash my face"})[0]
	
	:markdown
		this is a test [go home](/home.view)

	p 0.1. SYSTEM SECURITY PLAN PREPARED BY

	p 0.2. REVISION HISTORY

	table
		th: p Date
		th: p Description
		th: p System Version
		th: p Authors
		th: p Complete
		tr
		td: p #{released}
		td: p #{proj.Task}
		td: p #{ver}
		td: p pms: !{distro.pm} users: !{distro.user}
		td: p #{proj.Complete}

	p 1. INFORMATION SYSTEM NAME/TITLE

	p This System Security Plan (SSP) ... blah blah
	p!= show(pocs,{Role:"pm"},null)

	p 1.1 All my apps

	p!= show(apps,null,"Released,Host,Nick")

	p  1.2 All my users
	p!= show(users,null,"Client,Joined,QoS,Liked")

	table
		th: p System Plan Identifier (SPID)
		th: p Information System Name
		th: p System Abbreviation
		th: p System Dump
		th: p System of Record Notice (SORN)
		tr
		td: p #{info.a[0].SPID}
		td: p #{title}
		td: p #{nick}
		td: p #{json(info)}
		td: p #{info.a[0].SORN}

	p 1.1 SCOPE AND APPLICABILITY

	p This document applies to all NGA owned, controlled, outsourced, blah blah

	p 2. INFORMATION SYSTEM CATEGORIZATION

	p 2.1 INFORMATION TYPES

	h1 Chapter 1

	p The Lorenz Equations \(x^2 = 0\)
	p.
		$$
		\begin{align}
		\dot{x} & = \sigma(y-x) \\
		\dot{y} & = \rho x - y - xz \\
		\dot{z} & = -\beta z + xy
		\end{align}
		$$
	br
	p Impressive 'eh
	p.
		\[
		J_\alpha(x) = \sum\limits_{m=0}^\infty \frac{(-1)^m}{m! \, \Gamma(m + \alpha + 1)}{\left({\frac{x}{2}}\right)}^{2 m + \alpha}
		\]

	h1 Chapter 2
	//
		#grid.News(
			path="/news.db",
			page=25,dims=dims,
			cols="New.c,To.t,Starts.d,Stay.n,Message.h,Category.x")

	//
		#grid.InfoTypes(
			path="/json.db",
			cols="NISTtype,NISTid,Confidentiality,Integrity,Availability",
			links=`key=apps.Totem.Info.b` )

	//
		each sec,idx in secs
		#fit(class=`x${idx}. ${sec.title}`)
			p= sec_body
			if sec.grid
				#grid(class=`g${idx}`,path=`/${sec.grid}.db`,cols="To,Message")

	//- UNCLASSIFIED
